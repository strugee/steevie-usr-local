#!/bin/sh

if [ $(id -u) -ne 0 ]; then
        echo 'deploy-pumabot: not invoked as root; bailing'
        exit 1
fi

sudo -u root npm install -g pump-io/pumabot && systemctl daemon-reload && systemctl restart hubot-pump
